git clone url .

git remote add upstream url           ( upstream-from which we clone the repository )

git remote -v                         

git branch                            ( branch name )

git checkout                          ( branch name )

code .                                ( direct to vs code )

git status                            ( info. related to working of your file system )

git add -A                            

git commit -m” “


git init

git push -u origin main
OR
(if there is some warning then refer this) force push
git push -u origin main -- force
